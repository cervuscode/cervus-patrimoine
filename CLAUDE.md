# CLAUDE.md — Cervus Patrimoine

Contexte projet pour les sessions Claude. À lire en premier.

## Projet
- **Cervus Patrimoine** — cabinet de gestion de patrimoine **indépendant** (CGP).
- Site **Next.js 14 (App Router) / TypeScript / Tailwind CSS**, déployé sur **Vercel** (deploys auto depuis `main`).
- Repo GitHub : **cervuscode/cervus-patrimoine**.
- Domaine de prod : **cervuspatrimoine.fr** (SANS tiret — ne jamais écrire `cervus-patrimoine.fr`).
- Email contact : `contact@cervuspatrimoine.fr`. ORIAS n° 25006770.

## Architecture (dossiers)
- `src/app/(site)/` — pages publiques avec Navbar + Footer (route group). Home = `(site)/page.tsx`. Pages légales : mentions-legales, politique-de-confidentialite, reclamations, conflits-d-interets, qui-sommes-nous, services, reserver. Contient aussi `simulateur-interets-composes/` (outil SEO public, voir section dédiée).
- `src/app/simulateur-per/` — simulateur PER (HORS route group, layout minimal).
- `src/app/simulateur-assurance-vie/` — simulateur Assurance-vie (HORS route group, calqué sur le PER).
- `src/app/api/` — routes serveur (capture, CRM, PDF).
- `src/lib/` — moteurs de calcul + intégrations (`fiscal-engine.ts`, `av-engine.ts`, `interets-composes.ts`, `pipedrive.ts`).
- `src/components/` — globaux (Navbar, Footer, AnimatedSection, ProfilCarousel, SchedulerPipedrive…).
- `public/` — assets (`/videos/hero-15s.mp4`, `/images/...`). Déposer images/vidéos ici.

## Outil RDV Conseiller (`app.cervuspatrimoine.fr`) — chantier en cours
Espace web authentifié **interne au cabinet** (poste de pilotage des RDV clients), même repo / même projet Vercel que le site public, servi sur le **sous-domaine `app.cervuspatrimoine.fr`**. Doc de cadrage complet : `docs/MASTER_OUTIL_RDV.md` (découpage en 14 lots 0→13, invariants : Pipedrive = source de vérité, fiche client unique, `fiscal-engine.ts` consommation seule, code client `C-0042` à l'écran, Supabase seulement au Lot 13).
- **Lot 0 TERMINÉ ET VALIDÉ EN PROD** (socle auth + coquille, RIEN d'autre : pas de Pipedrive, pas de simulateurs) :
  - **Auth Google OAuth via NextAuth v4** (`next-auth@^4.24`) : `src/lib/auth.ts` (`authOptions`) + handler `src/app/api/auth/[...nextauth]/route.ts`. Google provider avec `hd=ALLOWED_DOMAIN` (UX, **non sécurisant**) **+ défense en profondeur** dans le callback `signIn` : revérif **server-side** que l'email retourné finit par `@${ALLOWED_DOMAIN}` (rejette si `hd` falsifié → `?error=AccessDenied`). Session JWT, **aucun mot de passe stocké**. `pages.signIn`/`error = "/login"` (URLs PUBLIQUES sans préfixe `/app`).
  - **Middleware routing par hostname** : `src/middleware.ts`. Logique **STRICTEMENT identique dev/prod, aucune branche `NODE_ENV`**. `APP_HOSTS = { app.cervuspatrimoine.fr, app.localhost }` (dev local via `app.localhost`, résolu auto par les navigateurs). Sur hôte app → rewrite `/...` vers `/app/...` SAUF `/api`, `/_next`, `/app` (déjà préfixé) ; pose `X-Robots-Tag: noindex, nofollow`. Sur hôte public → **404 sur tout `/app/*`** (l'espace conseiller n'y est jamais servi ; site public intact). `/api/auth/*` jamais réécrit (callback OAuth `https://app.cervuspatrimoine.fr/api/auth/callback/google` préservé).
  - **Coquille** : route group `src/app/app/(conseiller)/` (le `/app` apparaît dans l'URL interne, pas le `(conseiller)`). `layout.tsx` (metadata `noindex`, fond sombre charté), `page.tsx` (accueil protégé via `getServerSession` → redirect `/login` si pas de session, sinon « Espace Conseiller » + déconnexion), `login/page.tsx` (bouton Google + message d'erreur propre `AccessDenied`). Boutons client `src/components/conseiller/SignInButton.tsx` / `SignOutButton.tsx` (`signIn`/`signOut` next-auth/react, sans `SessionProvider` car tout passe par `getServerSession` côté serveur).
  - **`noindex`** : triple — root layout global déjà actif + metadata du layout conseiller + header `X-Robots-Tag` du middleware.
  - **Charte** : ajout couleur Tailwind `cervus-gold-dark: #5D4738`.
  - **Variables d'env** (Vercel Prod+Preview) : `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXTAUTH_SECRET` (`openssl rand -base64 32`), `NEXTAUTH_URL` (`https://app.cervuspatrimoine.fr`), `ALLOWED_DOMAIN` (`cervuspatrimoine.fr`). Documentées dans `.env.local.example`.
  - **Non touché** : aucun fichier du site public hors `middleware.ts` (créé) / `tailwind.config.ts` (additif) / `.env.local.example` (additif) ; `fiscal-engine.ts` et `pipedrive.ts` intacts.
  - **Critère de validation** : compte `@cervuspatrimoine.fr` → accès accordé ; Gmail externe → refus propre (retour `/login` + message), pas d'erreur brute.
  - **VALIDÉ EN PROD (20 juin 2026)** : connexion `@cervuspatrimoine.fr` fonctionnelle ; **accès externe bloqué nativement par l'écran de consentement Google configuré « Internal »** (le compte externe ne peut même pas atteindre le consentement → couche supplémentaire AVANT le callback `signIn` server-side, qui reste la barrière de défense en profondeur) ; DNS `app.cervuspatrimoine.fr` propagé ; déploiement Vercel réussi. **Actions hors-repo Lot 0 toutes faites** (client OAuth Google Cloud avec consent screen Internal, sous-domaine Vercel, CNAME Squarespace, 5 vars d'env Vercel Prod+Preview, redéploiement).
- **Lot 1 TERMINÉ** (lecture Pipedrive + vue client + recherche + code client + panneau persistant ; PAS de fiscal-engine = Lot 2, PAS de note de synthèse = Lot 3 ; PAS de création manuelle de client — décision actée, Auguste crée la fiche dans Pipedrive puis la recherche dans l'outil) :
  - **Champs Pipedrive créés via API (28 au total)** : groupe « Découverte RDV » = miroir exact des champs « Simulation » du périmètre §4.3 + champs Découverte-only. Convention : suffixe ` (Découverte RDV)` (l'API ne crée pas de sections visuelles). **Person (16)** : miroirs `Statut marital`, `Nombre d'enfants`, `Statut professionnel`, `Revenu imposable`, `Salaire mensuel`, `Revenu conjoint`, `Année de naissance`, `Âge retraite`, `TMI` + Découverte-only `Parts fiscales`, `Garde`, `Profession`, `AV existante`, `Autre épargne`, `Immobilier`, `Notes libres`. **Deal (8)** : miroirs `Foncier`, `BNC`, `BIC`, `Profil investisseur`, `Versement initial`, `Versement mensuel` + `Code client` (système). Types calqués sur la source (varchar/double/monetary/text). Notes libres = champ **Person** (« une zone de notes globale par client », MD §4.2), code client = champ **Deal**.
  - **Règle Simulation/Découverte RDV** (MD §4.1, invariant #2) : le site public n'écrit JAMAIS en Découverte RDV (jamais d'écrasement silencieux). À l'affichage, l'outil **pré-remplit visuellement** un champ Découverte vide avec la valeur Simulation (badge « pré-rempli (Simulation) »), **SANS écriture auto**. Seuls les champs réellement modifiés par Auguste (dirty) sont écrits en Découverte au clic « Enregistrer » → deviennent prioritaires aux ouvertures suivantes (badge « corrigé en RDV »). Simulation reste lecture seule.
  - **Code client `C-0042`** (`src/lib/pipedrive.ts`) : **Volet A** = génération auto à la source, dans `syncProductToPipedrive` (le goulot unique de création de Deal du site public PER/AV), **uniquement à la création d'un nouveau deal** via flag `dealCreated` → `assignClientCodeOnCreate(dealId)` : scan paginé de tous les deals (`computeNextClientCode`, `C-` + max+1 zéro-paddé 4), **strictement non-bloquant** (try/catch qui avale tout, no-op si champ absent), **jamais régénéré**. **Décision actée : un code PAR DEAL** (pas par personne — pas de recopie inter-deals). **Volet B** = pour un deal créé à la main sans code : bouton « Générer un code » (affiche le prochain via `/api/rdv/next-code`, **n'écrit pas**) + copie 1 clic ; Auguste colle dans Pipedrive ; « Lier la fiche » vérifie via `/api/rdv/link-code` puis recharge.
  - **Registre de champs unique** : `src/lib/rdv-fields.ts` (isomorphe, aucun secret) — `{label, entity, kind, section, simName?, decName}` pilote lecture/écriture/rendu. **Aucun nom de champ Pipedrive en dur ailleurs.**
  - **API routes protégées** `src/app/api/rdv/*` (`_guard.ts` → `getServerSession` → 401 sinon, barrière même depuis l'hôte public car le middleware ne 404 que `/app/*`) : `search`, `client` (GET vue), `next-code`, `link-code`, `save` (PUT Découverte uniquement, jamais Simulation).
  - **UI** (`src/app/app/(conseiller)/`) : `page.tsx` = recherche champ libre (`ClientSearch`, **jamais de dropdown** — invariant #5) ; `client/[personId]/page.tsx` = vue client (server `force-dynamic` → `getClientView`, hydrate le contexte). Composants `src/components/conseiller/` : `RdvClientProvider` (contexte partagé : pré-remplissage + dirty par champ + `save()`), `PersistentPanel` (**mobile** = bottom sheet ; **desktop** = panneau latéral droit ; lanceur fixe bas affichant le code ; **auto-save à la fermeture** si dirty), `DealSelector` (**sélecteur multi-deal explicite** — décision actée, auto-sélection si un seul deal), `DiscoveryField` (Sim lecture seule + Découverte éditable + badge), `NotesField` (**édition inline dans le panneau** — voir chantier Simulateurs conseiller ci-dessous : flouté par défaut + œil → textarea inline + re-floutage), `ClientCodeBadge`/`CopyButton` (code + « Copié ! »), `GenerateCodeBox` (Volet B), `SaveBar` (« Enregistrer » explicite + indicateur dirty + toast), `DiscoverySections`. **Mobile-first**, charte respectée. Panneau monté dans le layout **uniquement si session** (jamais sur `/login`).
  - **Tests** : 5 nouveaux tests Volet A dans `src/lib/__tests__/pipedrive.test.ts` (scan+1, padding, non-régénération, no-op si champ absent). **81 tests verts** (76 fiscal + pipedrive). Les 9 tests pipedrive existants intacts (le mock meta sans `Code client` → Volet A no-op).
  - **Token Pipedrive en local** : `.env.local` (gitignoré) avec `PIPEDRIVE_API_TOKEN` + `PIPEDRIVE_DOMAIN=cervus` (nécessaire pour lire/écrire les champs en dev).
- **Chantier Simulateurs conseiller TERMINÉ** (structure + pattern du simulateur conseiller, PER comme 1er exemple ; consomme `fiscal-engine.ts` en LECTURE SEULE, ne le modifie pas — le Lot 2 ci-dessous reste à faire pour la propagation d'état partagé) :
  - **⚠️ RÈGLE DE SÉCURITÉ COURANTE (remplace la logique « masquer au cas où on partage la fenêtre principale »)** : la fenêtre/onglet principal de l'outil RDV **n'est JAMAIS partagé à l'écran en visio**. **Seul** l'onglet dédié « Présentation client » (point D ci-dessous) est partagé. Le floutage des notes n'est donc plus une barrière mais une **protection anti-étourderie**.
  - **Notes simplifiées (annule le pattern onglet dédié du Lot 1)** : l'ancienne route `/app/notes/[personId]` + composant `NotesEditor` **SUPPRIMÉS** (grep préalable, aucune ref orpheline). `NotesField` réécrit = **édition SUR PLACE dans le panneau persistant** : contenu réel rendu mais **flouté** (`blur-[6px]`) + icône œil ; clic œil → **textarea inline éditable** (pas de modal, pas d'onglet) ; **re-floutage** (a) au clic « Enregistrer » (save notes-only `/api/rdv/save`) ou (b) après **2 min d'inactivité de frappe** (timer `useRef` réarmé à chaque frappe) — **sauvegarde auto avant re-floutage si modifs en attente** (ne jamais perdre une note tapée en RDV). Pas d'auth/route séparée (protection simple : état local + CSS + timer).
  - **A. Timeout session par inactivité (1h)** : `src/lib/auth.ts` → `session: { strategy:"jwt", maxAge:3600, updateAge:0 }` (**fenêtre glissante** NextAuth : chaque accès session re-signe le JWT → exp repoussé d'1h). `ConseillerSessionShell` (`SessionProvider` **sans** `refetchInterval` — un polling re-signerait le token sans activité réelle et annulerait le timeout) + `SessionActivityGuard` : prolonge sur activité réelle (`mousedown/keydown/scroll/touchstart`, throttle 5 min → `getSession()`), et **arme un timer sur la date `expires`** → à l'échéance `signOut({callbackUrl:"/login?reason=timeout"})`. `login/page.tsx` affiche « Session expirée par inactivité, reconnectez-vous. » sur `?reason=timeout`. Pas d'event explicite à la fermeture d'onglet (inutile : onglet fermé = plus d'activité = expiration à +1h). Guard monté dans le layout `(conseiller)` uniquement.
  - **B. Accueil enrichi** : `src/app/app/(conseiller)/page.tsx` + section « Simulateurs » → `SimulateurGrid` (lit le registre `src/lib/conseiller-sims.ts`, extensible : ajouter un simulateur = ajouter une entrée). Clic depuis l'accueil = mode AUTONOME.
  - **C. Simulateur PER conseiller, 2 modes** (`src/components/conseiller/PerQuickSim.tsx`, composant client partagé ; helper pur isomorphe `src/lib/per-quick.ts` = `computePerQuick` consomme `calculerTMI`/`economieFiscaleAnnuelle`/`projectionPER` + encode/decode query). **Deux routes distinctes** : **AUTONOME** `src/app/app/(conseiller)/simulateur-per/page.tsx` (statique, aucun accès Pipedrive, aucune écriture) ; **CONNECTÉ** `.../client/[personId]/simulateur-per/page.tsx` (`force-dynamic` → `getClientView`, **pré-remplissage priorité Découverte RDV > Simulation** via les ids `rdv-fields`, deal le plus récent ; code client en évidence, pas le nom ; bouton « Présenter au client »). **AUCUNE écriture Pipedrive depuis le simulateur** (il calcule, il ne sauvegarde pas). Bouton d'accès « Simulateur PER » ajouté sur `ClientView`. TMI = `calculerTMI(R, parts, parts)` (taux marginal au quotient).
  - **D. Mode présentation (premier jet, ⚠️ REMPLACÉ par le chantier E/F/G ci-dessous)** : route mono-sim `presentation-per/[personId]` + `PerPresentation.tsx` + bouton « Présenter au client » sur `PerQuickSim` — **TOUS SUPPRIMÉS** au chantier E/F/G au profit de l'espace présentation unifié. Le principe (onglet `_blank` sans `noopener`, query params, `postMessage` same-origin avec vérif `origin`+`type`, garde-fou opener fermé) est **conservé et généralisé** dans le pont F.
  - **Tests** : `src/lib/__tests__/per-quick.test.ts` (10 tests de câblage). `fiscal-engine.ts` non touché.
- **Chantier E/F/G TERMINÉ** (Simulateur PER complet + refonte espace Présentations + Référence fiscale ; consomme `fiscal-engine.ts` en LECTURE SEULE) :
  - **E. Simulateur PER complet (Lot 5 du MD)** — distinct du PER rapide (conservé). Moteur isolé `src/lib/per-sortie.ts` (`computePerSortie`, consomme `projectionPER`/`calculerTMI`/`impotReel` + `per-quick`). **3 sorties** : (1) capital intégral = versements à la tranche sélectionnée + plus-value au PFU 30 % ; (2) fractionnement 20 ans, retrait fixe `capital/20`, solde non-sorti à 2 %/an, **résiduel d'intérêts versé en année 20 et taxé PFU** ; (3) rente viagère **Abeille Retraite Plurielle** (`TABLE_CONVERSION_ABEILLE`, TGF05, taux technique 0 %, éd. oct. 2024 ; rattachement à la tranche de naissance la plus proche **sans interpolation** ; **âges 64/67 seulement**, `taux64 null` pour 1957) + fiscalité **RVTO** (`BAREME_RVTO` complet ; fraction imposable 40 % à 64/67 ; IR = **option (b)** `impotReel(R+fraction)−impotReel(R)` ; PS 17,2 %). **RÈGLES FISCALES VALIDÉES par Auguste — ne rien inventer ; toute table à mettre à jour = ce seul fichier.** UI `PerFullSim.tsx`, 2 routes `simulateur-per-complet` (autonome) + `client/[personId]/simulateur-per-complet` (connecté, prefill Découverte>Simulation, tranche défaut = TMI courante). Aucune écriture Pipedrive. Tests `per-sortie.test.ts` (9 : 1 cas/sortie + lookups). Entrée registre « Simulateur PER complet ».
  - **F. Refonte espace « Présentations clients »** — remplace le résultat figé mono-sim par un **espace unifié navigable**. `src/lib/presentation-bridge.ts` (isomorphe) : sépare **IDENTITÉ** (revenu/parts/année naissance — lecture seule, rapatriable) vs **HYPOTHÈSES** (versement/horizon/profil/tranche/âge conversion — éditables, jamais écrasées) ; protocole `PRESENT_MSG_REQUEST`/`PRESENT_MSG_IDENTITY` (avec `simId`) ; encode/decode URL. **Source d'identité = la fiche** : `PresentationBridge.tsx` (monté sur `ClientView`, dans `RdvClientProvider`) = bouton **« Accéder aux présentations clients »** (ouvre `presentation/[personId]` en `_blank`) + **répondeur** postMessage lisant `useRdvClient()` (donc « Actualiser » rapatrie une correction du panneau). `PresentationSpace.tsx` = menu des sims (registre, `identityKeys`/`hypothesisKeys`), **navigation in-tab**, état scindé `identity` (MAJ par Actualiser **uniquement**) / `hypoBySim` (MAJ par éditions locales **uniquement** → hypothèses **préservées** à l'Actualiser). Vues `presentation/PerQuickPresentation.tsx` + `presentation/PerFullPresentation.tsx` (id→composant). `ClientView` : bouton présentation proéminent + accès PER rapide **et** complet en connecté.
  - **G. Référence fiscale 2026 (consultation/vérif croisée, PAS un moteur)** — `src/lib/bareme-reference-2026.ts` (isomorphe, **valeurs en dur** du doc interne `Cervus_Tableau_Fiscal_2026_v4.pdf` ; **ne consomme PAS fiscal-engine**) : `TABLE_IMPOT_NET` (13 situations × 15 revenus → impôt net + TMI), `TABLE_ECONOMIE_PER` (versement × TMI), `BAREME_2026_PARAMS`, `lookupImpotReference` (interpolation linéaire de l'impôt net + snap TMI, **clairement étiqueté « ≈ référence »**). UI `ReferenceFiscale.tsx` (sélecteur situation + champ revenu libre + grille PER + params), route `reference-fiscale` (groupe conseiller, **sans client requis**). Accès : carte « Outils » sur l'accueil + lien dans le panneau persistant (visible même sans client). **Mise à jour annuelle = remplacer ce seul fichier.**
  - **Tests** : **100 verts** (76 fiscal + pipedrive + 10 per-quick + 9 per-sortie + 5 interets-composes + av). `fiscal-engine.ts` non touché.
- **Lot 2 TERMINÉ** (état fiscal partagé + menu statut + fix Actualiser ; consomme `fiscal-engine.ts` en LECTURE SEULE) :
  - **`src/lib/fiscal-state.ts`** (pur isomorphe) = LA règle « calculé une seule fois » : `computeFiscalState(src)` → `{ revenuNetImposable, partsBase, partsTotal, tmi }`. **Priorité champ explicite** : `revenuImposable`/`partsFiscales` renseignés → utilisés tels quels (parts : convention `partsBase=partsTotal`) ; sinon **repli** `calculerRevenuImposable({salaireMensuel×12, revenuConjoint, foncier, bnc, bic})` / `calculerParts(mapStatutToParts(statut), nbEnfants)`. Appelé des DEUX côtés (contexte client mémoïsé + routes sim connectées côté serveur) → une seule logique, jamais dupliquée.
  - **`RdvClientProvider`** expose `fiscalState` via `useMemo([personDraft, dealDraft])` → recalcul auto à chaque changement des champs sources (revenu, parts, statut, enfants, salaire, conjoint, foncier, bnc, bic). État mémoire pur, jamais sauvegardé.
  - **Consommation** : `computePerQuick(input, opts?: {tmi?})` accepte une TMI injectée (mode connecté → `fiscalState.tmi`) ; absent (autonome/présentation sans contexte) → calcul local inchangé. Routes sim connectées : `computeFiscalState` une fois côté serveur → prefill `revenu=revenuNetImposable`, `parts=partsTotal`, `fiscalTmi`/`trancheSortie défaut = tmi`. `PresentationBridge` : identité = `fiscalState` (revenu net, parts, **tmi**) transmise en query + `postMessage` ; `PerQuickPresentation` consomme `identity.tmi` (zéro recalcul côté présentation). **Plus aucun `calculerTMI` dispersé dans les sims** ; `defaultTrancheSortie` ne reste que pour l'autonome.
  - **Menu déroulant « Statut marital »** : `DiscoveryField` rend un `<select>` (`STATUT_MARITAL_OPTIONS` : Célibataire/Marié(e)/Pacsé(e)/Divorcé(e)/Veuf(ve)) au lieu d'un texte libre. Le champ **Pipedrive reste texte libre** (non touché) ; l'outil écrit le libellé exact → la lecture `mapStatutToParts` est une **correspondance directe** (repli tolérant conservé pour vieilles valeurs). Veuf(ve)→celibataire (1 part). Appliqué partout où le champ est édité (panneau + vue client, via `DiscoveryField`).
  - **Ligne fiscale dans le panneau** : sous le code client, `TMI X % · Y parts · revenu net Z €`, uniquement si client chargé et revenu>0 — recalcul live (vérifié : revenu 30k→100k → TMI 30→41).
  - **Fix bug « Actualiser »** : `PresentationBridge` répond avec `identityRef.current` (ref réassignée à chaque render, identité dérivée de `fiscalState` recalculé) → lit TOUJOURS l'état courant à la réception du message, **sans reload**. Vérifié par harness iframe (édition revenu 30k→50k→99k puis Actualiser → 50k/99k, TMI 30→41).
  - **Tests** : `fiscal-state.test.ts` (7 : priorité champ/repli, mapping statut). **107 verts**. `fiscal-engine.ts` non touché.
- **Ajustements H/I TERMINÉS** (consomment `fiscal-engine.ts` en LECTURE SEULE) :
  - **H. Menu déroulant « Profil investisseur »** : `DiscoveryField` rend un `<select>` (Prudent/Équilibré/Dynamique = `PROFIL_LABELS`) pour le champ `profil`, comme le statut marital (Lot 2). Champ **Pipedrive resté texte libre** ; l'outil écrit le libellé exact → lecture `pickProfil` en correspondance directe. Appliqué partout (panneau + vue client via `DiscoveryField`).
  - **I. Slider taux de performance** : `taux?: number` (décimal) **optionnel** ajouté à `PerQuickInputs`/`PerSortieInputs` ; `computePerQuick`/`computePerSortie` → `resolveTaux(profil, taux)` (slider si fourni, clampé **0–10 %** via `TAUX_MIN`/`TAUX_MAX`/`clampTaux`, sinon défaut `TAUX_PAR_PROFIL[profil]`). **N'est PAS ajouté aux encode/decode des moteurs** (tests roundtrip intacts) ; ajouté à `HypoValues` (clé `tx`) + `hypothesisKeys` des 2 sims. Composant partagé `TauxSlider.tsx` (`<input type="range">` classe `.cervus-range` **tactile** + saisie % exacte). Câblé dans `PerQuickSim`, `PerFullSim`, `PerQuickPresentation`, `PerFullPresentation` ; **changer de profil ré-aligne le taux sur le défaut du profil**, puis ajustable librement. Les 2 modes (autonome/connecté).
  - **J. (horizon 64 ans) ANNULÉ** — le champ « Âge retraite (Découverte RDV) » alimentait déjà le calcul d'horizon dans les routes connectées/`PresentationBridge` ; logique inchangée.
  - **Tests** : +1 assertion `computePerQuick({taux})` (priorité + clamp 0–10 %). **108 verts**. `fiscal-engine.ts` non touché.
- **Lot 3 TERMINÉ** (note de synthèse de RDV → Note sur le Deal Pipedrive ; consomme `fiscal-engine.ts` en LECTURE SEULE via l'état déjà calculé, ne le touche pas) :
  - **`src/lib/sim-history.ts`** (pur isomorphe, aucun secret, ne consomme PAS fiscal-engine) : type `SimRecord` (union discriminée `per-quick`/`per-full` : `{id, ts, simId, label, inputs, result}`), `signatureOf` (dédup), `summarizeRecord` (résumé 1 ligne HTML), `formatSyntheseNote` (note HTML légère complète). Les résultats sont déjà calculés par les simulateurs et seulement mémorisés/formatés ici.
  - **Historique de session en mémoire** : vit dans `RdvClientProvider` (`simHistory: SimRecord[]`), exposé via `recordSim(draft)` + `generateSyntheseNote()` + `generatingNote`. **Dédup CONSÉCUTIF** (n'ajoute que si la variante diffère du dernier enregistrement → couvre le double-clic identique, conserve A→B→A = 3 variantes chronologiques). **Reset dans `setClient`** (changement/réouverture de client). **Purement en mémoire, JAMAIS persisté au fil de l'eau** ; écrit dans Pipedrive uniquement au clic explicite.
  - **Capture MANUELLE explicite** (⚠️ remplace l'ancienne capture automatique debouncée, retirée car un slider générait des variantes intermédiaires parasites) : bouton partagé **`KeepResultButton.tsx`** « Garder ce résultat » (mobile-first, cible tactile `min-h-[44px]`, pleine largeur mobile / `sm:w-auto self-end` desktop, feedback transitoire « Gardé ! » ~1,5 s comme `CopyButton`). Présent dans les 4 contextes : `PerQuickSim`/`PerFullSim` en mode CONNECTÉ (prop `client` ; clic → `recordSim(perQuickDraft/perFullDraft(...))`) et `PerQuickPresentation`/`PerFullPresentation` (clic → `onRecord(draft)`). **Aucune capture sans ce clic.** Bouton grisé si versement mensuel ET initial = 0. Mode autonome (sims sans `client`) = pas de bouton.
  - **Bouton** : `SyntheseNoteButton.tsx` dans le **panneau persistant** sous `SaveBar` (visible si client + deal actif ; sinon message « Sélectionnez un deal »). Au clic → compile Découverte (lignes par section via `RDV_FIELDS` + ligne « Fiscalité (calculée) » depuis `fiscalState`, **seulement les renseignées**) + historique → `formatSyntheseNote` → `POST /api/rdv/note`. Confirmation inline « Note ajoutée au deal ✓ » / erreur, **sans recharger**. Mobile-first.
  - **Écriture Pipedrive** : `addDealNote(dealId, contentHtml)` dans `pipedrive.ts` (API Notes `POST /notes` via `pdPost`, `{content, deal_id}`) — **n'écrit AUCUN champ Découverte**. Route `src/app/api/rdv/note/route.ts` protégée par `requireConseiller` (valide `dealId` + contenu non vide). Note Pipedrive = HTML léger (titres + listes). Historique vide → section « Simulations consultées » **omise** (note = Découverte seule).
  - **Capture depuis l'espace Présentations (cross-onglet)** : les variantes gardées dans l'onglet **Présentations clients** (fenêtre séparée, hors contexte React de la fiche) sont transmises à l'historique de session via `window.opener` + `postMessage` same-origin (même canal que « Actualiser »). Message **`PRESENT_MSG_RECORD`** (`presentation-bridge.ts`) : au clic « Garder ce résultat », `PerQuickPresentation`/`PerFullPresentation` appellent `onRecord(draft)` → `PresentationSpace.onRecord` poste **immédiatement** `opener.postMessage({type:PRESENT_MSG_RECORD, draft}, origin)` et **renvoie un booléen** (`false` si opener fermé → la vue affiche « Fiche fermée ») → `PresentationBridge` (récepteur, monté dans le contexte) vérifie `event.origin` + `type` + `draft.simId ∈ {per-quick, per-full}` → `recordSim(draft)` (dédup consécutif). Mode autonome non concerné (pas d'opener). **Builders purs `perQuickDraft`/`perFullDraft`** (`sim-history.ts`) = source UNIQUE du mapping result→draft, partagée par les sims connectés ET les vues de présentation.
  - **Tests** : `src/lib/__tests__/sim-history.test.ts` (13 : builders, dédup, résumés per-quick/per-full, rente indisponible, en-tête+date, lignes vides ignorées, omission section vide, échappement HTML). **121 verts**. Câblage cross-onglet (capture manuelle) vérifié par harnais iframe navigateur (9/9 : clic→capture immédiate, double-clic identique→dédup, A→B→A=3, vérif origin, garde type/simId, opener fermé→false). Aperçu visuel du bouton vérifié mobile (335×44) + desktop. `fiscal-engine.ts` non touché.
  - **⚠️ Test réel d'écriture Pipedrive NON exécuté en local** : le `PIPEDRIVE_API_TOKEN` de `.env.local` renvoyait 401 (expiré) → format vérifié par le rendu du vrai code, écriture validée en PROD par Auguste (token Vercel à jour).
- **Lot 4 TERMINÉ** (Simulateur d'impôt — interface au-dessus de `fiscal-engine.ts`, **ZÉRO ajout au moteur** : il calcule déjà l'IR complet, on ne fait que l'exposer ; consomme `fiscal-engine.ts` en LECTURE SEULE) :
  - **Particularité architecturale** vs les sims PER : **TOUT est « hypothèse »**, aucun champ en lecture seule — y compris revenu et situation familiale sont librement éditables (scénarios « et si… »). Donc `identityKeys: []` dans le registre (toléré : ces deux clés sont **purement documentaires**, consommées nulle part — vérifié par grep).
  - **`src/lib/impot-sim.ts`** (pur isomorphe, aucun secret) : `ImpotInputs` (statut libellé, nbEnfants, garde, demiPartHandicap, revenuImposable), `GardeRegime` (`classique`/`alternee`/`isole`) + `GARDE_OPTIONS`, `resolveParts()` (statut marital + garde + nb enfants → statut effectif `calculerParts` + `ctx.caseT`), `computeImpot()` → `{impotNet, tmi, partsBase, partsTotal, tauxMoyen, plafonnementActif, statutEffectif, caseT}`, `normalizeStatutLabel`/`normalizeGarde` (texte Découverte → libellé/régime exact), `formatPourcent`. Consomme `impotReel`/`calculerTMI`/`calculerParts`/`impotBrut` (moteur) + `mapStatutToParts`/`STATUT_MARITAL_OPTIONS` (`fiscal-state`).
  - **Régime de garde = UN seul sélecteur** (pas Garde + Case T en deux cases qui se contredisent) : affiché **uniquement si non marié/pacsé ET ≥1 enfant** → *Garde principale · Garde alternée (`garde_partagee`, quart de part) · Parent isolé (`parent_isole` + case T)*. Mutuellement exclusifs (la case T suppose une garde exclusive ; le moteur ne gère pas le quart-de-part case T). **Demi-part handicap** = case à cocher séparée (comme le simulateur public, `ctx.handicap` + `calculerParts(..., true)`).
  - **Résultat (niveau « pédagogique » validé par Auguste)** : impôt net + TMI en avant, puis **parts retenues + taux moyen** + **badge « plafonnement du quotient familial actif »**. Le plafonnement est **détecté SANS toucher au moteur** : compare l'avantage enfants réel (`impotReel` sans/avec) à l'avantage non plafonné théorique (`impotBrut` sans/avec). Pas de mention décote (`applyDecote` non exportée → on ne duplique pas ses constantes).
  - **Registre/UI** : entrée `"impot"` dans `conseiller-sims.ts` (la grille d'accueil + la fiche se mettent à jour seules) ; composant `src/components/conseiller/ImpotSim.tsx` (2 modes autonome/connecté, calque `PerQuickSim`, `KeepResultButton` en connecté) ; routes `simulateur-impot` (autonome) + `client/[personId]/simulateur-impot` (connecté, **prefill priorité Découverte RDV > Simulation** via `computeFiscalState` pour le revenu + `normalizeStatutLabel`/`normalizeGarde` pour la situation, **mais tout reste éditable** — le prefill n'est qu'un point de départ) ; bouton « Simulateur d'impôt » ajouté sur `ClientView`. **Aucune écriture Pipedrive.**
  - **Espace Présentations** : 3ème simulateur du menu. `HypoValues` (`presentation-bridge.ts`) étendu d'**un sous-objet `impot` groupé** (additif, valeurs neutres, **ignoré par les vues PER**) + encode/decode (clés `is/ie/ig/ih/ir`). Vue `presentation/ImpotPresentation.tsx` (résultats en avant, inputs éditables secondaires, **n'utilise PAS `ClientIdentity`** → « Actualiser » sans effet sur cet onglet, cohérent avec « tout est hypothèse »). `PresentationSpace.VIEWS["impot"]` + `PresentationBridge` (seed `hypo.impot` depuis la fiche + `simId:"impot"` accepté dans le handler `PRESENT_MSG_RECORD`).
  - **Capture note de synthèse** : `sim-history.ts` étendu — `ImpotRecord` (`simId:"impot"`), builder `impotDraft()`, cas `impot` dans `signatureOf` (dédup) + `summarizeRecord` (1 ligne HTML : situation → impôt net, TMI, taux moyen, mention plafonnement). Capture manuelle « Garder ce résultat » dans les 2 contextes (connecté `recordSim` direct ; présentation `onRecord` → postMessage), bouton grisé si revenu = 0.
  - **⚠️ Commentaire obsolète du moteur** : les valeurs « ~ » en pied de `fiscal-engine.ts` (ex. « impotReel(30000,1,1) → ~2 022 €, TMI 11 % ») datent d'un **barème antérieur** (seuil 30 % différent) ; le moteur **actuel** rend 2 104 € / **TMI 30 %** (30 000 > 29 579). Les tests s'alignent sur les sorties réelles du moteur, **seule source de vérité** (moteur non touché).
  - **Tests** : `src/lib/__tests__/impot-sim.test.ts` (croisement barème 2026 : 30k/70k/90k/200k + plafonnement actif 70k vs inactif 60k, garde/case T/handicap, normalisation) + impôt ajouté à `sim-history.test.ts`. **141 verts** (121 + 20). `fiscal-engine.ts` non touché.
  - **Vérif visuelle** : harnais temporaire (supprimé) → aperçu **mobile + desktop** des 3 contextes (autonome, connecté pré-rempli C-0042 marié/1 enfant/70k → impôt **5 401 €** / TMI 30 % / 2,5 parts / taux moyen 7,7 % / badge plafonnement / bouton Garder, vue présentation), sélecteur de garde conditionnel vérifié, zéro erreur console. **Routes connectées/présentation réelles non testées en local** (auth Google + token Pipedrive) → validation PROD par Auguste, comme les lots précédents.
- **Lot 6 TERMINÉ** (Illustration du mécanisme de réduction d'impôt — visuel pédagogique avant/après par tranche ; consomme `fiscal-engine.ts` en LECTURE SEULE, **ZÉRO ajout au moteur**) :
  - **But** : montrer comment un versement PER déductible fait « descendre » le revenu imposable à travers les tranches (0/11/30/41/45 %). Distinct du Simulateur d'impôt (Lot 4, qui ne donne qu'un montant) : celui-ci est volontairement **visuel**.
  - **`src/lib/reduction-impot.ts`** (pur isomorphe) : `ReductionInputs = ImpotInputs & { versementPer }` ; `computeReduction(input)` → `{ revenuAvant, revenuApres, montantEfface, avant, apres, economie, tmiChange, slicesAvant, slicesApres, tranchesEffacees }`. **Importe `computeImpot`/`resolveParts`/`GARDE_OPTIONS`/normalisations de `impot-sim.ts`** (zéro duplication de la logique garde/parts). **Chiffres impôt/TMI/économie TOUJOURS issus de `computeImpot` (= `impotReel`, vrai moteur)** ; le découpage `decoupeParTranche(revenu, parts)` est **purement illustratif** (seuils × parts pour raisonner en euros foyer). **`TRANCHES_AFFICHAGE`** = miroir des 4 seuils barème 2026 **recopiés en constante d'affichage** (pas d'export `TRANCHES` du moteur → « zéro ajout au moteur » strict ; MAJ annuelle = ce seul tableau, comme `bareme-reference-2026.ts`). Couleurs charte crème→bronze foncé.
  - **Particularité (comme Lot 4)** : **tout est « hypothèse »**, aucun champ figé (revenu, situation familiale, versement tous éditables). `identityKeys: []` dans le registre (documentaire).
  - **Visuel `ReductionStacks.tsx`** (source UNIQUE du rendu, partagée sim + présentation) : 2 colonnes empilées en **blocs CSS** (`column-reverse`), **côte à côte MÊME sur mobile** (la comparaison des hauteurs est tout l'intérêt) ; bloc **« fantôme » en pointillés** = portion de revenu sortie des tranches hautes (« −X € sortis de la tranche Y % ») ; légende couleur=taux. Hauteur de référence 300 px, les 2 colonnes partagées par la même échelle (`revenuAvant`).
  - **UI** : `ReductionImpotSim.tsx` (2 modes autonome/connecté, calqué sur `ImpotSim`, `KeepResultButton` en connecté, grisé si revenu **ou** versement = 0) ; `presentation/ReductionPresentation.tsx` (HypoPills/HypoNumber, n'utilise PAS `ClientIdentity` → « Actualiser » sans effet, cohérent « tout est hypothèse »). Routes `simulateur-reduction-impot` (autonome) + `client/[personId]/simulateur-reduction-impot` (connecté, prefill Découverte>Simulation via `computeFiscalState` ; **versementPer pré-rempli depuis `versementInitial` de la fiche**, 0 si absent ; tout reste éditable). Bouton « Illustration réduction d'impôt » sur `ClientView`. **Aucune écriture Pipedrive.**
  - **Espace Présentations** : 4ème simulateur du menu. `HypoValues` étendu d'un **sous-objet dédié `reduction: ReductionInputs`** (additif, clés URL `xs/xe/xg/xh/xr/xv`, ignoré par les autres vues) car il porte un champ de plus (`versementPer`) que le sous-objet `impot`. `PresentationSpace.VIEWS["reduction-impot"]` + `PresentationBridge` (seed `hypo.reduction` depuis la fiche + `simId:"reduction-impot"` accepté dans `PRESENT_MSG_RECORD`).
  - **Capture note de synthèse** : `sim-history.ts` étendu — `ReductionRecord` (`simId:"reduction-impot"`), builder `reductionDraft()`, cas dans `signatureOf` (dédup) + `summarizeRecord` (1 ligne : revenu → versement → impôt avant/après, économie/an, TMI avant→après si elle change).
  - **Tests** : `src/lib/__tests__/reduction-impot.test.ts` (cas validation 60k/6k célib → économie **1 800 €**/an TMI 30 % inchangée ; versement franchissant un seuil → TMI 30 %→11 % ; couple 2 parts seuils doublés ; versement>revenu borné à 0 ; revenu nul ; chiffres = vrai moteur) + réduction ajoutée à `sim-history.test.ts`. **151 verts** (141 + 10). `fiscal-engine.ts` non touché.
  - **Vérif visuelle** : harnais temporaire (supprimé) → **mobile (375 px) + desktop** des 3 contextes. Cas C-0042 célib 60k/6k → colonnes côte à côte, bloc fantôme « −6 000 € sortis de la tranche 30 % », impôt **11 104 € → 9 304 €**, économie **1 800 €/an**, taux moyen 18,5 %→17,2 %, légende, bouton Garder ; zéro erreur console. **Routes connectées/présentation réelles non testées en local** (auth Google + token Pipedrive) → validation PROD par Auguste.
- **Lot 7 TERMINÉ** (Comparateur Assurance-vie / PER ; consomme `per-sortie.ts` + `av-engine.ts` en LECTURE SEULE, **ZÉRO ajout aux moteurs**) :
  - **Convention « EFFORT NET ÉGAL » (Option B, validée Auguste)** : le conseiller saisit **un seul effort net mensuel** (+ apport initial net) = ce que le client sort de sa poche. **AV** reçoit l'effort tel quel ; **PER** reçoit l'effort **majoré** `effort / (1 − TMI/100)` (l'économie d'impôt complète automatiquement la différence, intégrée DÈS le versement). **Aucun cumul d'économie ajouté en fin** : on compare directement `per-sortie.sortie1.capitalNet` (PER) à `av-engine.capitalNetSansCervus` (AV). Rendement **identique des deux côtés** piloté par le profil (3/4/5 %), **pas de slider de taux**. **AV de référence = `capitalNetSansCervus` UNIQUEMENT** (jamais la stratégie de rachat, jamais le mot « purge » exposé).
  - **`src/lib/comparateur-av-per.ts`** (pur isomorphe) : `ComparateurInputs` (identité revenu/parts/marie + hypothèses effortNetMensuel/effortNetInitial/horizon/profil/trancheSortie) ; `computeComparateur(input, opts?:{tmi?})` → facteurLevier, perMensuel/perInitial, economieMensuelle/Initiale, `per.capitalNet`, `av.capitalNetSans`, **`gagnant: "per"|"av"|"egal"`**, ecart, ecartPct. TMI injectée (connecté, Lot 2) ou `calculerTMI` (autonome). `verdictComparateur(result)` = message pédagogique **3 régimes** (PER devant / **quasi-parité ≤ 1 % → souplesse AV** / AV devant). `av.primesVersees` recalculé en brut (PAS `primesVerseesTotal`, contaminé par les rachats).
  - **⚠️ Vérification du critère MD (faite avant code, vrais moteurs)** : effort net 210 €/mois, équilibré, 20 ans, célib, tranche sortie = TMI. **TMI 30 → PER 76 407 € vs AV 70 328 € (PER +8,6 %)** ✅ ; **TMI 11 → PER 70 855 € vs AV 70 328 € (écart +0,7 % ≤ 1 % → verdict « egal », message souplesse AV)** ; **TMI 0 → AV devant** (aucun levier, PER taxe la PV au PFU). La règle « TMI 11 → AV » **n'émerge pas en chiffre brut** (quasi-parité) → servie honnêtement par le **message** (souplesse, épargne disponible), pas un verdict numérique forcé. Seuil de quasi-parité = **1 %** (`SEUIL_QUASI_PARITE_PCT`).
  - **UI** : `ComparateurAvPer.tsx` (2 modes, **pas de slider de taux**, `KeepResultButton` connecté grisé si effort = 0) + `ComparateurStacks.tsx` (2 barres côte à côte PER vs AV, même échelle, gagnant surligné — les deux en quasi-parité — blocs CSS mobile-first) + `presentation/ComparateurPresentation.tsx` (revenu/parts/TMI = `identity` rafraîchissable comme les vues PER ; hypothèses éditables). Mention pédagogique obligatoire de l'hypothèse « effort net / avantage intégré dès le versement ». Routes `simulateur-comparateur-av-per` (autonome) + `client/[personId]/...` (connecté, prefill `computeFiscalState` + `marie` du statut, effort = versements fiche, tranche sortie défaut = TMI). **Aucune écriture Pipedrive.**
  - **Espace Présentations** : 5ème simulateur. `HypoValues` étendu d'un sous-objet `comparateur: ComparateurHypo` (= `ComparateurInputs` sans revenu/parts, ceux-ci venant de `ClientIdentity` ; clés URL `cem/cei/ch/cpr/cts/cma`). `PresentationSpace.VIEWS["comparateur-av-per"]` + `PresentationBridge` (seed depuis la fiche + `simId` accepté dans `PRESENT_MSG_RECORD`).
  - **Capture note** : `ComparateurRecord` + `comparateurDraft()` + cas `signatureOf`/`summarizeRecord` (effort net → PER net vs AV net, gagnant, TMI, écart).
  - **Tests** : `src/lib/__tests__/comparateur-av-per.test.ts` (facteur levier exact 210→300 à TMI 30 ; TMI 30 → per ; TMI 11 → egal ; TMI 0 → av ; horizon < 8 ans conso av-engine corrigé ; TMI injectée ; apport initial majoré) + comparateur ajouté à `sim-history.test.ts`. **164 verts** (154 + 10). Aucun moteur touché.
  - **Vérif visuelle** : harnais temporaire (supprimé) → **mobile + desktop** des 3 contextes. Connecté TMI 30 → barre PER (76 407 €) nettement > AV (70 328 €), verdict « Avantage PER +6 079 € » ; présentation TMI 11 → barres quasi égales, « Produits quasi équivalents » (souplesse AV) ; autonome vide. Zéro erreur console. **Routes connectées/présentation réelles non testées en local** (auth + token) → validation PROD par Auguste.
- **Refonte fiche client TERMINÉE** (suppression du panneau persistant + réorganisation de la fiche + encadré fiscal de synthèse ; consomme `fiscal-engine.ts` en LECTURE SEULE, **ZÉRO ajout au moteur**) :
  - **Suppression du panneau persistant** : `PersistentPanel.tsx` **SUPPRIMÉ** (devenu inutile depuis que l'espace Présentations vit dans un onglet dédié — il n'y a plus de fenêtre principale partagée à l'écran). `ContributionsHautsRevenusPanel.tsx` **SUPPRIMÉ** (son contenu CEHR/CDHR migre dans le nouvel encadré fiscal). Layout `(conseiller)/layout.tsx` : import + montage `<PersistentPanel />` retirés (`RdvClientProvider` enveloppe désormais `{children}` seul). Compensations de hauteur du lanceur nettoyées (`pb-28` retiré de l'accueil + des 6 simulateurs conseiller ; `sticky bottom-16` de la `SaveBar` de `ClientView` → `sticky bottom-0`). Les `pb-28` des **simulateurs publics** (`simulateur-per`, `simulateur-assurance-vie`) sont sans rapport et conservés.
  - **Tout le contenu du panneau migre dans `ClientView`** (fiche unique de pilotage). `SyntheseNoteButton` (« Générer la note de synthèse ») déplacé du panneau vers le bas de la fiche ; `SaveBar`, `ClientCodeBadge`, `DiscoverySections`, `NotesField` déjà présents → réorganisés. La ligne fiscale inline (TMI · parts · revenu) et le lien « Référence fiscale 2026 » du panneau sont absorbés par le nouvel encadré.
  - **`SyntheseFiscale.tsx` (NOUVEAU)** — encadré fiscal de synthèse, affiché **uniquement si `client` chargé ET `fiscalState.revenuNetImposable > 0`**. Consomme `fiscalState` + `contributionsHR` (déjà dans le contexte) ; réutilise `impotReel` (fiscal-engine) pour l'impôt net et `decoupeParTranche`/`TRANCHES_AFFICHAGE` (exportés de `reduction-impot.ts`) pour les chips de tranches — **aucun nouveau calcul, aucune écriture**. Contenu : 3 chiffres clés (impôt net, TMI effective, taux moyen = impôt net / revenu net), **chips colorées par tranche** (0/11/30/41/45 %, couleurs charte, contraste texte géré par tranche, montant foyer par chip), puis bloc **CEHR/CDHR regroupé dans le même encadré** (séparateur, affiché seulement si `contributionsHR.concerne`), + lien « Référence 2026 » en tête. Mobile-first.
  - **`DiscoverySections.tsx` refacto** : l'export default monolithique (toutes sections + Notes en dernier) est remplacé par deux exports nommés `DiscoverySection({section})` (une section) et `NotesSection()` — la composition est désormais pilotée par `ClientView` pour intercaler Notes en tête et l'encadré fiscal au milieu. Prop `compact` supprimée (plus de panneau 1 colonne → grille `sm:grid-cols-2` partout).
  - **Ordre de la fiche** (`ClientView`) : (1) en-tête code + identité repliable, (2) `PresentationBridge`, `DealSelector`, liens simulateurs, `GenerateCodeBox` si besoin, (3) **Notes remontées en tête** (mécanisme œil/flou/2 min inchangé), (4) Situation, (5) Professionnel, (6) Revenus, (7) **`SyntheseFiscale`** (+ CEHR/CDHR), (8) Profil investisseur, (9) Épargne, (10) Patrimoine financier, (11) « Générer la note de synthèse » + indicateur nb simulations, (12) `SaveBar` « Enregistrer » sticky tout en bas.
  - **Non touché** : `RdvClientProvider` (tous les memos `fiscalState`/`contributionsHR`/`patrimoineFinancier` inchangés), `PresentationBridge`, `NotesField` (juste repositionné), `SaveBar`, `SyntheseNoteButton` (juste déplacé), routes API, simulateurs (hors retrait `pb-28`), `fiscal-engine.ts`.
  - **Tests** : aucun test ajouté (refonte UI pure, logique inchangée). **206 verts** (inchangé). tsc + lint OK.
  - **Vérif visuelle** : harnais temporaire (supprimé) → **mobile (375) + desktop** de la fiche refondue avec client mock (marié 2 enf, revenu 600 k → impôt net 223 048 €, TMI 45 %, taux moyen 37,2 %, 5 chips charte somme = 600 000 €, CEHR 3 000 € / CDHR / Total 226 048 €). Ordre 1→12 vérifié de bout en bout (Notes floutées en tête → … → bouton synthèse → Enregistrer). Zéro erreur console. **Routes connectées réelles non testées en local** (auth Google + token Pipedrive) → validation PROD par Auguste, comme les lots précédents.
- **Lot 8 TERMINÉ** (Plafond de versement PER : TNS/Madelin + cumul + UI fiche ; consomme `fiscal-engine.ts` en LECTURE SEULE, **ZÉRO ajout au moteur**) :
  - **⚠️ CORRECTION PASS 2026 (préalable au Lot 8)** : les constantes plafond PER utilisaient le **PASS 2025 (47 100 €)** — FAUX pour des **versements 2026**. Corrigé dans `per-quick.ts` : `PASS_2025`→**`PASS_2026 = 48 060 €`**, `PER_PLANCHER = 4 806` (10 % × PASS), `PER_PLAFOND_MAX = 38 448` (10 % × 8 PASS). Ré-export `per-sortie.ts` mis à jour (`PASS_2026`). Commentaires/descriptions de `per-quick.test.ts` rafraîchis (assertions inchangées car sur constantes). **Règle = PASS de l'année de VERSEMENT.** Vérif sources DGFiP : `tgvI.m` cale bien son millésime sur le PASS de l'année de revenus (LIM_PERPMIN 4637 = 10 % × PASS 2024 46 368 ; LIM_PERPMAX 37 094 = 10 % × 8 × 46 368) → cohérent. **Choix assumé/documenté en commentaire** : côté TNS/Madelin (art. 154 bis) le PASS 2026 est strictement correct (déduction sur le bénéfice de l'année même du versement) ; côté salarié (art. 163 quatervingts) le plafond officiel de l'avis serait sur PASS N-1, mais l'outil retient PASS 2026 comme **estimation prospective** cohérente avec son usage pédagogique/indicatif (ne calcule déjà pas les reliquats N-3).
  - **`src/lib/plafond-per.ts` (NOUVEAU, pur isomorphe)** — RÉUTILISE les constantes PASS 2026 + `computePlafondPER` de `per-quick.ts` (zéro duplication). `computePlafondTNS(bnc, bic)` → Madelin : `10 % × min(bénéfice, 8 PASS) + 15 % × max(0, min(bénéfice, 8 PASS) − PASS)`, plancher 10 % PASS (4 806), **cap 8 PASS sur les DEUX termes** (conforme art. 154 bis ; sans ce cap un bénéfice > 8 PASS surévaluerait le plafond) → max théorique **88 911 €** (`PER_TNS_PLAFOND_MAX`). `computePlafondTotal({revenuImposable, foncier, bnc, bic, estTNS})` → `{plafondSalarie, plafondTNS, plafondTotal}` : **cumul** salarié (sur revenu net − foncier, plancher inclus) + TNS (sur BNC+BIC, 0 si non-TNS). TNS pur = plancher salarié 4 806 + Madelin.
  - **Switch TNS = champ Pipedrive booléen** (décision actée) : champ Person **`Est TNS (Découverte RDV)`** créé À LA MAIN par Auguste (type **Texte** — Pipedrive n'a pas de booléen natif ; stocke `"Oui"`/vide). Ajouté au registre `rdv-fields.ts` (`id:"estTNS"`, person/text, section `epargne`, **nouveau flag `hiddenInDiscovery:true`**) → read/write/hydratation/dirty/save **hérités gratis** de la machinerie `RDV_FIELDS` ; le script `create-rdv-fields.ts` le créerait (idempotent) mais le champ existe déjà. `DiscoverySections.tsx` filtre `!hiddenInDiscovery` (jamais rendu comme `DiscoveryField`).
  - **Contexte** : memo **`plafondPER`** dans `RdvClientProvider` (comme `fiscalState`/`contributionsHR`) = `computePlafondTotal` sur `fiscalState.revenuNetImposable`, `foncier`/`bnc`/`bic` (dealDraft), `estTNS` → recalcul auto. Expose `plafondPER` + `estTNS` (dérivé `personDraft["estTNS"]==="Oui"`) + `setEstTNS(b)` (= `setValue("estTNS", b?"Oui":"")`). Pur mémoire, jamais sauvegardé (le switch, lui, est persisté via la SaveBar comme tout champ Découverte).
  - **UI `PlafondVersementPER.tsx` (NOUVEAU)** — bloc dans `ClientView` **entre `epargne` et `patrimoine`**. Switch iOS (Tailwind pur, charte, `role="switch"`). Affichage conditionnel : non-TNS = « Plafond de déductibilité PER : X € (10 % du revenu net imposable) » ; TNS = 3 lignes « Plafond salarié / Plafond Madelin (TNS) / Plafond total cumulé ». Mention reliquats N-3 non calculés. Alerte non bloquante de dépassement = **`PlafondPerAlert` réutilisé** (généralisé avec props optionnelles `plafondOverride`/`footnote`, rétro-compatible → PerQuickSim/PerFullSim inchangés) comparée au **plafond total**. Mobile-first.
  - **Synthèse fiscale** : `SyntheseFiscale.tsx` — ligne discrète en bas « Plafond PER disponible : X € » (ou « X € salarié + Y € TNS = Z € total » si TNS), depuis `plafondPER`. Pas de répétition du détail.
  - **Tests** : `src/lib/__tests__/plafond-per.test.ts` (NOUVEAU, 10 : constantes PASS 2026, Madelin plancher/1-8 PASS/cap 88 911 €/exactement 8 PASS, cumul non-TNS/mixte/TNS pur/foncier exclu/borne salarié). **220 verts** (210 + 10). tsc + lint OK. `fiscal-engine.ts` non touché.
  - **Vérif visuelle** : harnais temporaire (supprimé) → **mobile (375) + desktop** du bloc avec 2 mocks. Salarié seul (revenu 90 k) → plafond **9 000 €**, versement 6 000 € dans la limite. TNS mixte (salaire 90 k + BNC 50 k) → salarié **9 000 €** + Madelin **5 291 €** = total **14 291 €**, versement 18 000 € → alerte dépassement **3 709 €**. Switch testé (salarié→TNS, BNC 0 → Madelin = plancher 4 806 €). Zéro erreur console. **Routes connectées réelles non testées en local** (auth Google + token Pipedrive) → validation PROD par Auguste.
- **Lot 9 TERMINÉ** (Pyramide de l'épargne — visuel pédagogique de répartition patrimoniale réelle vs cible ; **ne consomme AUCUN moteur**, ne fait que sommer/répartir des montants) :
  - **3 nouveaux champs Pipedrive (Person, monetary), section `patrimoine`, `hiddenInDiscovery` absent** (affichés normalement) — **créés À LA MAIN par Auguste**, **script non lancé pour eux** (idempotent, les skipperait) : `encoursFondsEuros` (`Encours AV fonds euros (Découverte RDV)`), `encoursPer` (`Encours PER (Découverte RDV)`), `capaciteEpargneMensuelle` (`Capacité d'épargne mensuelle (Découverte RDV)`). Ajoutés à `rdv-fields.ts` (30 champs désormais) → read/write/hydratation/save/prefill **hérités gratis** de la machinerie `RDV_FIELDS`. **Décision actée** : pas de champ « encours PER » avant ce lot (seuls `versementInitial/Mensuel` = Deal, paramètres de scénario) → on a créé `encoursPer` dédié (stock), **PAS** `plafondPER` (un plafond, pas un encours) ni `versementInitial` (proxy faux).
  - **`src/lib/pyramide-epargne.ts` (NOUVEAU, pur isomorphe)** — `PyramideInputs` (9 enveloppes + `capaciteEpargneMensuelle`), `computePyramide` → `{ patrimoineTotal, ciblePrecaution, surplus, niveaux[] }`. **4 niveaux** : N1 Précaution = `livretsReglementes + livretsBoostes + autreEpargne` ; N2 Projet = `encoursFondsEuros` ; N3 Long terme = `(encoursAv − encoursFondsEuros, UC clampée ≥ 0) + encoursPea + encoursPer` ; N4 Dynamique = `cto + crypto`. **`patrimoineTotal` = livrets + autre + encoursAv + PEA + PER + CTO + crypto** (le fonds euros s'annule entre N2 et N3). **Cibles** : N1 = `6 × capacité` (null si capacité 0 → statuts `neutre`, pyramide réelle quand même affichée) ; surplus = `max(0, total − ciblePrecaution)` réparti **40 % Projet / 40 % Long terme / 20 % Dynamique**. **Écart réel/cible** : `statutEcart(ratio)` → `ok` si `0,8 ≤ ratio ≤ 1,2`, `sur` si `> 1,2` (orange), `sous` si `< 0,8` (rouge), `neutre` si pas de cible (gris). Constantes `SEUIL_BAS=0.8`, `SEUIL_HAUT=1.2`, `MOIS_PRECAUTION=6`, `REPARTITION_SURPLUS`, `STATUT_COLORS`/`STATUT_LABELS`.
  - **Particularité (comme Lots 4/6)** : **tout est « hypothèse »** (les encours), aucun champ figé → `identityKeys: []` dans le registre. Vue présentation n'utilise PAS `ClientIdentity` (« Actualiser » sans effet).
  - **Visuel `PyramidsView.tsx` (NOUVEAU, source UNIQUE)** — 2 pyramides : gauche = **cible théorique** (« 6 mois » / 40/40/20), droite = **réelle** (montant €, % du total, **pastille d'écart** colorée). Sommet = DYNAMIQUE, base = PRÉCAUTION (sens classique). Technique : **4 couches en TRAPÈZE `clip-path`** (largeur 44 %→100 %) formant un triangle ; **texte en surimpression non rogné** (toujours lisible). Côte à côte desktop (`sm:grid-cols-2`), empilées mobile. `patrimoineTotal=0` → message neutre.
  - **UI** : `PyramideEpargneSim.tsx` (2 modes, saisie groupée par enveloppe, `KeepResultButton` connecté grisé si total=0) ; `presentation/PyramidePresentation.tsx` (HypoNumber par enveloppe) ; **`PyramideEpargneBloc.tsx`** = bloc inline sur la fiche (lit `patrimoineFinancier` du contexte, affiché si total>0), monté **après la section patrimoine**. Routes `simulateur-pyramide-epargne` (autonome) + `client/[personId]/simulateur-pyramide-epargne` (connecté, prefill depuis `patrimoineFinancier`/getClientView, tout reste éditable). Bouton « Pyramide de l'épargne » sur `ClientView`. **Aucune écriture Pipedrive.**
  - **Câblage** : `PatrimoineFinancier` (contexte) étendu (+`encoursFondsEuros`, `encoursPer`, `capaciteEpargneMensuelle`) + memo. Registre `conseiller-sims.ts` (entrée `pyramide-epargne`, 6ᵉ sim). `presentation-bridge.ts` : sous-objet `pyramide: PyramideInputs` (clés URL `yl1/yl2/yae/yfe/yav/ype/ypr/yct/ycr/ycap`) + `DEFAULT_HYPO` + encode/decode. `PresentationSpace.VIEWS["pyramide-epargne"]` + `PresentationBridge` (seed `hypo.pyramide` depuis `patrimoineFinancier` + `simId` accepté dans `PRESENT_MSG_RECORD`). `sim-history.ts` : `PyramideRecord` (résultat **réduit** : total + niveaux {key,label,montantReel,pctTotal,statut}), `pyramideDraft()`, cas `signatureOf` + `summarizeRecord` (1 ligne : total → répartition par niveau, marquage sur/sous-représenté).
  - **Tests** : `src/lib/__tests__/pyramide-epargne.test.ts` (NOUVEAU, 12 : répartition par niveau, AV UC clampée, total, cible 6 mois, surplus 40/40/20, capacité absente → neutre, surplus nul, seuils ±20 %, précaution sur-représentée) + pyramide ajoutée à `sim-history.test.ts` (3). **235 verts** (220 + 15). tsc + lint OK. Aucun moteur touché.
  - **Vérif visuelle** : harnais temporaire (supprimé) → **mobile (375) + desktop (1280)** des 3 contextes (autonome vide, connecté pré-rempli, présentation). Mock 157 000 € (précaution 55 000 € = 35 % **sur-représenté** orange ; projet 40 000 € = 25 % **sous-représenté** rouge ; long terme 50 000 € = 32 % **conforme** vert ; dynamique 12 000 € = 7,6 % **sous-représenté**), cible précaution 9 000 € / surplus 148 000 €. Pyramides côte à côte desktop / empilées mobile, trapèzes + pastilles d'écart, légende, bouton Garder. Zéro erreur console. **Routes connectées réelles non testées en local** (auth Google + token Pipedrive) → validation PROD par Auguste, comme les lots précédents.
- **Lot 10 TERMINÉ** (Résilience des marchés — 3 graphiques recharts comparant MSCI World / S&P 500 / Livret A / fonds euros + profils ; **ne consomme AUCUN moteur fiscal**, indexe/compose des séries historiques) :
  - **Données (`docs/market-data/`)** : seuls les **CSV dérivés annuels légers** committés (`msci-world-annual.csv`, `sp500-shiller-annual.csv` = valeur de **décembre** par an, 1969→2025) + `README.md` (sources/attribution). Les bruts (MSCI mensuel, Shiller `ie_data.xls`) **non committés** (trop lourds). `scripts/build-market-data.ts` (tsx, **dev-only, jamais importé**) régénère les CSV depuis les bruts locaux (traçabilité).
  - **`src/lib/market-data.ts` (NOUVEAU, pur isomorphe, valeurs EN DUR)** : `MSCI_WORLD`/`SP500` (déc. par an 1969→2025) ; `LIVRET_A_RATES` (taux **moyen annuel** Banque de France 1970→2026) ; `FONDS_EUROS_RATES` (France Assureurs, **série depuis 1994 seulement** — pas de série publique fiable avant, décision actée) ; taux fixes actuels `LIVRET_A_NOW = 1.5` (depuis fév. 2026), `FONDS_EUROS_NOW = 2.5` ; `PROFIL_RATES {prudent:3, equilibre:4, dynamique:5}` ; `SERIE_META` (couleurs charte), `SOURCE_MENTION` (mention obligatoire), `FONDS_EUROS_ANNOTATION`. **MAJ annuelle = ce seul fichier.**
  - **`src/lib/resilience-marches.ts` (NOUVEAU, pur isomorphe, AUCUN moteur)** : **Graph 1** `buildHistorical({base100, includeProfils})` (lignes annuelles 1970→2025 ; prix indexés, taux composés ; fonds euros base 100/10 000 en **1994**, absent avant ; profils composés depuis 1970). **Graph 2** `buildLumpSum({amount=10000, years=20, endYear=2025})` (capital unique, **vrais rendements année par année** ; fenêtre 2005→2025 ; `finals` par série). **Graph 3** `buildContribution({versementInitial, versementMensuel, horizonAnnees})` (**capitalisation mensuelle composée** au taux équivalent ; profils 3/4/5 % + Livret A 1,5 % + fonds euros 2,5 % via `G3_RATES`). Tous purs, `round2`.
  - **UI** : `MarchesChart.tsx` (recharts `LineChart` réutilisable, `connectNulls={false}`, formatteur €/indice, mobile-first `ResponsiveContainer`) ; `SeriesToggles.tsx` (pastilles on/off par série) ; `ResilienceMarchesSim.tsx` (2 modes, 3 sections, bascule Base 100/Nominal + toggle profils sur G1, inputs G3, `KeepResultButton` connecté, mention source sous chaque graphe) ; `presentation/ResiliencePresentation.tsx` (G1/G2 figés, G3 piloté par `hypo.resilience` ; pas de `ClientIdentity`). Routes `simulateur-resilience-marches` (autonome) + `client/[personId]/...` (connecté, prefill G3 : versements de la fiche + horizon = `ageRetraite − âge courant`, défaut 20). Bouton « Résilience des marchés » sur `ClientView` (7ᵉ sim). **Aucune écriture Pipedrive.**
  - **Câblage** : `conseiller-sims.ts` (entrée `resilience-marches`, `identityKeys:[]`, `hypothesisKeys` versements/horizon). `presentation-bridge.ts` : sous-objet `resilience: ResilienceInputs` (`DEFAULT_RESILIENCE_INPUTS` défini dans `resilience-marches.ts` pour éviter d'importer un composant client) + clés URL `rvi/rvm/rh`. `PresentationSpace.VIEWS["resilience-marches"]` + `PresentationBridge` (seed depuis la fiche + `simId` accepté dans `PRESENT_MSG_RECORD`). `sim-history.ts` : `ResilienceRecord` (capitaux finaux des 5 séries G3), `resilienceDraft()`, cas `signatureOf` + `summarizeRecord`.
  - **Changement mineur (en parallèle)** : champ **« Statut professionnel »** → menu déroulant dans `DiscoveryField.tsx` (même pattern que statut marital / profil ; champ Pipedrive **reste texte libre**). Options : `Salarié / TNS / Dirigeant salarié / Profession libérale / Fonctionnaire / Retraité / Sans activité`. Un ancien « Indépendant » (Simulation) s'affiche vide → re-sélection (décision actée).
  - **Tests** : `src/lib/__tests__/resilience-marches.test.ts` (NOUVEAU, 16 : intégrité séries, base 100 = 100 en 1970, Livret A/fonds euros composés conformes, fonds euros démarre 1994, nominal/profils, lump-sum 2005→2025 + finals = vrais ratios/1,05^20, projection mensuelle 4 % → ×1,04/an, ordre des taux, `G3_RATES` alignés) + résilience ajoutée à `sim-history.test.ts` (3). **254 verts** (235 + 19). tsc + lint OK. Aucun moteur fiscal touché.
  - **Vérif visuelle** : harnais temporaire (supprimé) → **mobile (375) + desktop (1280)** des 3 graphes × 3 contextes. G1 base 100 (actions ×88 vs Livret A/fonds euros plats, fonds euros dès 1994) + bascule **nominal** (échelle → 1 M, MSCI domine) + tooltip multi-séries. G2 : 10 000 € de 2005→2025 → MSCI **51 465 €** / S&P **54 300 €** / Livret A **13 974 €** / fonds euros **16 689 €** / Dynamique **26 533 €** (= 10 000 × 1,05²⁰). G3 : 200 €/mois sur 20 ans (versé 48 000 €) → Prudent **65 371 €** / Équilibré **72 768 €** / Dynamique **81 161 €** / Livret A **55 877 €**. Mention source sous chaque graphe. Zéro erreur console. **Routes connectées réelles non testées en local** (auth Google + token Pipedrive) → validation PROD par Auguste.

## Autres simulateurs (idées futures, non priorisées)
Pistes repérées en veille, listées ici tant qu'elles ne sont pas un lot numéroté (cf. MD §9, note « onglet Autres simulateurs »). Ne deviennent un lot qu'une fois priorisées par Auguste.
- **Bilan Patrimonial Automatisé** (= Lot 14 du MD, non cadré en détail) : synthèse patrimoniale + IFI + succession/DMTG + plus-value immo + projection retraite + flux financiers, livrable PDF multi-pages. Calcul déterministe (pas d'IA générative), ampleur de plusieurs nouveaux `fiscal-engine.ts`. **Détail complet : `docs/MASTER_OUTIL_RDV.md` §14.**

## /reserver
Page hébergeant le **Pipedrive Scheduler** (iframe, `SchedulerPipedrive.tsx`). TOUS les CTA RDV pointent vers `/reserver`. Calendly a été entièrement retiré.

## Simulateur PER (existant, NE PAS CASSER)
- Parcours : `simulateur-per/components/SimulateurForm.tsx` orchestre 16 écrans `Q*.tsx` (Q0→Q15). État unique `SimulateurData` (`types.ts`), navigation conditionnelle.
- Moteur : `src/lib/fiscal-engine.ts` (parts, revenu imposable, TMI, projection PER, économie fiscale) — **validé, 76 tests** (`src/__tests__/fiscal-engine.test.ts`).
  - **Barème 2026** (LF 2026, revenus 2025, commit `fc18e0b`) : tranches 11600/29579/84577/181917 (inchangées vs 2025), `PLAFOND_PAR_DEMI_PART = 1807`, décote célib 897/seuil 1983, couple 1483/seuil 3278, coef 0,4525.
  - **Décote** : `applyDecote` renvoie l'impôt net = `max(0, impotBrut − décote)` (un ancien bug renvoyait le *montant* de la décote au lieu de l'impôt net → faussait tous les foyers en zone de décote/bas revenus ; corrigé dans ce même commit).
  - **Plafonnement QF différencié et extensible** : `plafondQuotientFamilial(partsBase, partsTotal, ctx)` calcule le plafond comme une SOMME par catégorie. `PlafondContext = { caseT?: boolean; handicap?: boolean }`. Case T (parent isolé, 1er enfant = part entière) → `PLAFOND_CASE_T = 4262` ; demi-parts au-delà → 1807 chacune ; standard → toutes demi-parts à 1807 (garde alternée : quart de part → 903,5 € par cohérence arithmétique). `impotReel`/`calculerTMI` acceptent un 4e param `ctx` optionnel (rétro-compatible, défaut `{}`).
  - **Demi-part handicap/invalidité FAITE** (commit `d86713d`, source DGFiP GP 125 / BOFiP BOI-IR-LIQ-20-20-20, mécanique CGI en DEUX TEMPS — NE PAS coder en plafond unique majoré). (1) `calculerParts(statut, nbEnfants, demiPartHandicap=false)` : 3e param optionnel → `+0,5` sur `partsTotal` UNIQUEMENT (jamais `partsBase` → l'écart base↔total crée l'avantage ; célib 0 enf + handicap = 1/1,5). (2) **TEMPS 1 plafonnement** : `ctx.handicap` n'a AUCUN effet dans `plafondQuotientFamilial` — la demi-part est déjà dans `partsSupp` donc déjà plafonnée à 1807 par le calcul standard (NE PAS rajouter +1807 → double-comptage). (3) **TEMPS 2 réduction complémentaire** : dans `impotReel`, APRÈS plafonnement et AVANT la décote, si `ctx.handicap && economieReelle > plafondTotal` → `reductionCompl = min(REDUCTION_COMPL_HANDICAP=1801, economieReelle − plafondTotal)`, `impot −= reductionCompl`, borné ≥ 0. Si le plafond ne mord pas → AUCUNE réduction. Ordre garanti : plafonnement → réduction compl. handicap → décote. NB : cas quart de part (900,50 €) NON géré (la case n'ajoute qu'une demi-part). Réf. officielle testée : BOFiP §185 couple 4 enf dont 1 invalide 130k → **10 758 € au centime** ; écart célib 0 enf 200k SANS/AVEC = 3608 € (1807 + 1801).
  - Câblage prod : `SimulateurForm.compute()` passe `calculerParts(statut, nbEnfants, data.demiPartHandicap)` + `ctx = { caseT: statut === 'parent_isole', handicap: data.demiPartHandicap }` (statut dérivé via `effectiveStatut`, car `data.statut` ne vaut JAMAIS `parent_isole` — la case T vient de l'écran `QGarde` → `gardeParentale`). Case à cocher « Invalidité ou handicap (demi-part supplémentaire) » sur l'écran situation `QStatut.tsx` (sous les boutons statut, visible par TOUS → couvre le célibataire invalide sans enfant). Champ `SimulateurData.demiPartHandicap` (bloc Situation, défaut false). `data.demiPartHandicap` voyage dans le payload `/api/submit` + `/api/early-contact` (data/computed envoyés tels quels, aucun câblage serveur) ; mention PDF discrète « · demi-part invalidité ». **Brevo/Make/Pipedrive non touchés** (pas d'attribut CRM ajouté). `preview-pdf` = démo figée → `ctx = {}`, `demiPartHandicap: false`.
- Résultats : `ResultPage.tsx` (stats + AreaChart recharts + CTA `/reserver`).
- PDF : `src/app/api/submit/PdfDocument.tsx` (@react-pdf/renderer).

## Simulateur Assurance-vie (nouveau, front terminé)
- Moteur : `src/lib/av-engine.ts` — **VALIDÉ, 7 tests** (`src/lib/__tests__/av-engine.test.ts`). Capitalisation mensuelle, optimisation des plus-values (rachat partiel/abattement + réinvestissement), seuil **150 000 € apprécié sur les primes NETTES** (`base`), taux IR **7,5 %/12,8 %** au prorata, `gainNetCervus = capitalNetAvec − capitalNetSans`. Marge de déclenchement ×1,1, seuil de pertinence 250 € → `purgeUtile`. Expose `courbe` (trajectoires) + `debugAV()`.
  - **⚠️ Fiscalité de sortie conditionnée au seuil des 8 ans (correctif, déployé séparément avant le Lot 7)** : `sortie(state, marie, dureeAnnees)` applique le régime selon l'antériorité. **`dureeAnnees >= 8`** = régime favorable INCHANGÉ (abattement 4 600/9 200 € + PFL 7,5 %/12,8 % au prorata 150k). **`dureeAnnees < 8`** = PAS d'abattement, **PFU 12,8 % sur TOUTE la PV** (option standard, jamais l'option barème) ; PS 17,2 % dans tous les cas → 30 % au total. Frontière : exactement 8 ans = régime favorable. Hypothèse documentée en commentaire : tous les contrats simulés sont **post-27/09/2017** ; le régime des contrats antérieurs (PFL 35 %/15 %) est **hors périmètre, non modélisé**. **Avant ce correctif, le moteur appliquait toujours le régime > 8 ans** → résultat trop favorable pour les horizons courts (le slider public descend à 2 ans). `ResultPageAV.tsx` → `netLiquidatif(valeur, base, marie, annee)` aligné sur la même règle (par année, seuil 8 ans) pour que le graphe de valeur nette reste cohérent avec l'en-tête. Capital brut inchangé (même accumulation quel que soit l'horizon). **Slider `min={2}` conservé** (calcul désormais juste sur 2-40) ; mention pédagogique < 8 ans proposée mais non encore décidée.
- Parcours : `components/SimulateurFormAV.tsx` — **un seul écran de saisie groupée** (`QParametresAV` : versements, horizon 2–40 ans, profil, situation) puis capture (`QIdentiteAV`, `QEmailAV`, `QTelephoneAV`). Calcul live via `calculerAV`.
- Résultats : `components/ResultPageAV.tsx` — hero bronze foncé sur le chiffre clé, cartes capital, graphique en **valeur nette** (avec ≥ sans), CTA `/reserver`. Si `purgeUtile=false` → scénario unique + encadré transparence (jamais d'opposition "avec Cervus" trompeuse).
- ✅ **Capture CRM AV branchée (étape 3 FAITE, committée `08c14d9`)** : `QEmailAV` → `check-contact {produit:"AV"}` ; `QTelephoneAV` → OTP réel (send-otp/verify-otp `produit:"AV"`) + mark-pending ; `SimulateurFormAV` → early-contact-av (écran tel) puis submit-av. Consentements ajoutés à `AVFormData`.
- ✅ **PDF AV FAIT** : `src/app/api/submit-av/PdfDocumentAV.tsx` (calqué sur `PdfDocument` PER, charte Cervus, scénario unique si `purgeUtile=false`, jamais le mot "purge", mention indicative/non contractuelle). Généré en base64 et joint au payload Make (`pdf` + `nom_fichier`) dans **submit-av ET early-contact-av**.

## Simulateur Intérêts composés (public, SEO — `/simulateur-interets-composes`)
- Outil **100 % public** : AUCUNE capture/OTP, AUCUNE route API, AUCUN Make/Brevo/Pipedrive, AUCUN PDF. Calcul live côté client.
- Page : `src/app/(site)/simulateur-interets-composes/page.tsx` (server component — metadata SEO "calcul intérêts composés", JSON-LD WebApplication + FAQPage, H1 unique, CTA, contenu éditorial H2/H3 ~750 mots, mention). Hérite du **noindex global** (aucune exception).
- Interactif : `SimulateurInteretsComposes.tsx` (client) — sliders avec **saisie manuelle au clic** sur la valeur ; fréquence mensuel/annuel ; inflation togglable ; imposition forfaitaire des gains (`Aucune`/`PFU 30 %`/`PS 17,2 %`, sur les GAINS seuls, **aucune enveloppe nommée**) ; graphique recharts aires empilées ; CTA principal → `/simulateur-per`, secondaire → `/reserver`.
- Sliders Capital initial & Versement : **échelle non-linéaire** (linéaire sur 75 % « zone de confort » 0→100 k€ / 0→2 k€, puis géométrique sur le dernier quart jusqu'à 5 M€ / 10 k€). Saisie manuelle exacte (clampée au max). Styles `.cervus-range` dans `globals.css`.
- Moteur : `src/lib/interets-composes.ts` — `calculerInteretsComposes()` pur, **6 tests** (`src/lib/__tests__/interets-composes.test.ts`). Capitalisation mensuelle au taux équivalent `(1+r)^(1/12)−1` ou annuelle, versements en fin de période, fiscalité sur gains, déflation inflation (€ constants). Conventions documentées en commentaire.

## Capture & CRM (routes API) — paramétrées "par produit" (défaut PER)
- `api/check-contact` — doublon détecté par **appartenance aux listes du produit** : `{email?, telephone?, produit?}`, défaut PER (#5/#6), AV (#11/#12). Un client PER peut faire une simu AV.
- `api/send-otp` / `api/verify-otp` — Twilio Verify (SMS). Whitelist de numéros de test. Migration listes Brevo paramétrée par produit : PER #6→#5, AV #11→#12 (`verify-otp` accepte `produit?`).
- `api/early-contact` (PER) / `api/early-contact-av` — fiche anticipée avant OTP : Brevo #6 (PER) / #11 (AV) + webhook Make `type=sans_otp` + Pipedrive (otpVerifie=false). **PER et AV avec PDF inline base64.**
- `api/submit` (PER) / `api/submit-av` — après OTP : Brevo #5 (PER) / #12 (AV) + webhook Make `type=otp_valide` + Pipedrive (otpVerifie=true). **PER et AV avec PDF inline base64.**
- `api/mark-pending` — marque la simu en attente si l'utilisateur quitte avant l'OTP (PER + AV).

### Sécurité / audit
- **Ciblage Pipedrive par email, jamais par ID du body** : les routes publiques d'écriture (`submit`, `early-contact`, `submit-av`, `early-contact-av`) ne lisent jamais de `personId`/`dealId` depuis le corps. La cible est résolue server-side via `findPersonByEmail` (`exact_match=true`) puis `findOpenDealForPersonAndProduct` (personId résolu + Produit). Aucun chemin ne touche un enregistrement autre que celui de l'email soumis ; réponses = `{ ok: true }` seul (jamais de données Pipedrive ni PDF renvoyés). Résidu connu (MD §11) : l'email étant non authentifié, écriture/écrasement possible sur l'enregistrement d'un email connu + email Make déclenché → à durcir (honeypot + validation) **avant la levée du `noindex`**. Atténué par rate-limiting IP.
- **`/api/pipedrive-sync` SUPPRIMÉE le 20 juin 2026** — route morte non utilisée (aucun appel front/Make/script, confirmé par grep), qui divulguait `personId`/`dealId` Pipedrive (`return { ok, ...result }`) sans auth ni rate-limit. Trouvée lors d'un audit de sécurité avant le Lot 2. Tout passe désormais par `submit`/`early-contact` (+ variantes AV).
- **Vigilance Lot 2+** : les routes `api/rdv/*` utilisent un `personId`/`dealId` venu du body — acceptable car protégées par `getServerSession` (équipe Cervus, déjà accès Pipedrive complet). À garder à l'esprit pour toute nouvelle écriture ajoutée.

## Pipedrive (`src/lib/pipedrive.ts`) — généralisé "par produit"
- Clés de champs custom résolues dynamiquement par nom (pas de hash en dur), cache 10 min.
- Cœur générique `syncProductToPipedrive` (param `produit` obligatoire) ; `syncToPipedrive` (wrapper PER : Produit "PER", Source "Simu-PER", titre "PER - …") et `syncAVToPipedrive` (Produit "AV", Source "Simu-AV", titre "AV - …", champs deal AV : Horizon, Capital net avec/sans, Gain net optimisé + réutilisés Versement initial/mensuel, Capital projeté).
- **Matching deal par PERSONNE + PRODUIT** (`findOpenDealForPersonAndProduct(personId, produit)`, produit OBLIGATOIRE) : filtre sur le champ texte **`Produit`** (comparaison trim+casse). On ne réutilise/déplace JAMAIS le deal d'un autre produit, on n'écrase JAMAIS son `Produit`. Une personne peut avoir un deal PER ouvert ET un deal AV ouvert simultanément. Les deux wrappers passent `produit` : `syncToPipedrive`→"PER", `syncAVToPipedrive`→"AV". Si aucun deal ouvert du produit (ou champ `Produit` introuvable) → création d'un nouveau deal.
- Routing pipeline selon OTP (commun PER/AV) : `Leads`/`Tel Validé` (OTP) vs `Leads sans OTP`/`Simulation effectuée`. Person = champs communs ; Deal = champs spécifiques produit.
- **Champ deal "Date simulation"** (type Date, résolu dynamiquement par nom) : écrit **à la création du deal** au format `YYYY-MM-DD` (date locale Europe/Paris, sans heure — `todaySimulationDate()`). Sert d'**ancre aux relances J+1/J+7/J+21**.
- **Échéancier de relances figé** : 3 champs deal Date **"Relance J1 le" / "Relance J7 le" / "Relance J21 le"** (groupe Simulation), écrits **à la création** = `Date simulation` + 1 / + 7 / + 21 jours calendaires (`addDaysToISODate()`, calcul robuste via `Date.UTC` → gère passages de mois/année/bissextile, format `YYYY-MM-DD`). Make lit ces dates au lieu de recalculer l'âge. Le champ texte **"Dernière relance envoyée"** est un témoin **écrit par Make** (jamais touché côté repo).
- **Set seulement si absent** (Date simulation + 3 relances) : sur un deal existant déjà daté, ces 4 clés sont retirées du payload de mise à jour (`stripDateSimIfAlreadySet`, ancré sur la présence de `Date simulation`) pour ne jamais réinitialiser l'échéancier (early-contact → submit, ou simu rejouée un autre jour). Présent sur PER et AV.

## Brevo & Make
- Brevo = CRM/listes/attributs. Make = envoi des emails (variables du **payload webhook** au moment de l'envoi, PDF inline base64 — pas de `PDF_URL` stocké).
- Attributs Brevo : communs (PRENOM, NOM, TELEPHONE…) ; PER (capital projeté, économie fiscale…) ; **AV créés** (`AV_CAPITAL_BRUT`, `AV_CAPITAL_NET_SANS/AVEC`, `AV_GAIN_NET`, `AV_HORIZON`, `AV_VERSEMENT_INITIAL/MENSUEL`, `AV_PROFIL`, `AV_SITUATION`). Listes AV #11/#12 créées.
- **Webhooks Make SÉPARÉS** : PER = `MAKE_WEBHOOK_URL`, AV = `MAKE_WEBHOOK_URL_AV` (scénario dédié isolé). Payload AV : `{produit:"AV", type, email, prenom, nom, telephone (submit-av seul), pdf (base64), nom_fichier, capital_brut, capital_net_sans/avec, gain_net, horizon, versement_initial/mensuel, profil_investisseur, situation}`. **PDF inline base64 inclus** (`pdf` exclu des logs).

## Conventions
- **Charte** : bronze `#795D48`, bronze foncé `#5D4738`, crème `#F2EDE8`, sombre `#0f0f0f`. Titres `font-cormorant`, corps `font-inter`. Boutons pilule `rounded-[50px]`. Cartes lisibles = fond crème + texte sombre (jamais petit texte sur fond sombre).
- **`noindex` ACTIF** dans `src/app/layout.tsx` (`robots: "noindex, nofollow"`) — **NE JAMAIS LE RETIRER** (site en pré-lancement).
- **Réglementaire (pré-COA)** : statut intermédiaire → ne pas recommander de produit précis ; simulations = "pédagogiques indicatives, non contractuelles". **Validation fiscale humaine requise avant mise en ligne** d'un moteur. **JAMAIS le mot "purge" côté client** (savoir-faire confidentiel) → utiliser "optimisation" / "Gain net avec Cervus Patrimoine".
- `AnimatedSection` : reveal au scroll (IntersectionObserver, rootMargin -12%, reduced-motion respecté).

## État d'avancement
**FAIT** : home complète, CTA → /reserver, simulateur PER en prod, moteur AV validé + parcours front + page résultats, câblage CRM AV "par produit" (`08c14d9`), **PDF AV** (`PdfDocumentAV` + branchement submit-av/early-contact-av), **simulateur public Intérêts composés** (SEO, lien navbar ajouté). Côté Brevo (listes #11/#12 + attributs AV_*) et Pipedrive (champ Produit "AV", champs deal AV) : créés.
**RESTE (actions hors-repo, finition AV)** :
1. **Template email AV** dans Brevo/Make.
2. **Cloner les 2 scénarios Make AV** (envoi immédiat + rattrapage 15 min) branchés sur `MAKE_WEBHOOK_URL_AV`, exploitant le PDF inline désormais envoyé.
3. **Renseigner `MAKE_WEBHOOK_URL_AV` dans Vercel** (sinon webhook AV no-op).

## Règles de travail
- **tsc + lint doivent passer** avant tout commit (`npx tsc --noEmit` + `npx next lint`).
- **Ne pas committer/pousser sans validation** de l'utilisateur (sauf demande explicite). Étapes moteur/fiscal = montrer les résultats d'abord.
- **Architecture "par produit"** pour scaler (PER, AV, puis PEA/CTO/SCPI…) : éviter le mono-produit en dur.
- **Un chantier à la fois.** Ne pas toucher au PER, à `/reserver`, aux pages légales, au `noindex`.
- Commits : messages clairs, terminés par la ligne `Co-Authored-By: Claude ...`.
